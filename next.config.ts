import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  images: {
    domains: ['encrypted-tbn0.gstatic.com', 'poetsandquants.com', 'static.vecteezy.com'],
  },
  // other config options here
};

export default nextConfig;
